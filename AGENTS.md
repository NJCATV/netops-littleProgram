# AGENTS.md

## 项目定位
智维助手小程序统一工单与用户体系项目，核心是统一工单池。项目方向是把现有智维助手升级为“统一用户体系 + 统一工单池 + OSS 工单融合 + 运维工具聚合入口 + 后续 Web 管理端”的平台。

OSS 工单必须融合进统一工单结构。OSS 不是独立外挂模块，也不得绕过统一工单池；必须先分析旧逻辑，再映射字段，再接入统一工单数据结构。

## 每次任务必读
每次任务开始前必须先阅读：

- `PROJECT_STATUS.md`
- `统一工单池_含OSS融合_技术与实施总规划.md`
- `PROJECT_PLAN.md`
- `DATABASE_DESIGN.md`
- `API_DESIGN.md`
- `TASK_LOG.md`
- `CHANGELOG.md`
- `QUESTIONS.md`

## 当前目录映射
- 当前旧小程序目录：`miniprogram/`
- 目标 uni-app 小程序目录：`miniapp/`。
- 后端目录：`backend/`
- 文档目录：根目录核心文档 + `docs/`
- 归档目录：`docs/archive/`
- OSS 参考代码：`docs/oss-reference/`

当前前端仍是微信小程序原生工程，不是标准 uni-app 工程。项目已决定前端优先迁移为 uni-app 实现；后续前端业务开发应优先落到新的 uni-app 工程，现有 `miniprogram/` 仅作为功能参考和过渡保留，不再作为长期主线继续扩展。

## 执行纪律
- 不允许擅自改变项目方向。
- 不允许把 OSS 做成独立孤岛。
- 不允许绕过统一工单池。
- 不允许直接照搬旧项目结构。
- 旧项目只能先分析、再映射、再融合。
- 前端新功能优先基于 uni-app 目标工程设计；不得继续扩大原生小程序旧结构的技术债。
- 不允许删除有价值历史内容；冲突、重复、过时内容先归档到 `docs/archive/`。
- 每个 Task 完成必须更新 `TASK_LOG.md` 和 `CHANGELOG.md`。
- 每个 Task 完成必须 `git commit`。
- 阶段完成必须 `git push`。
- 提交前必须检查 `git status`，避免把无关业务代码混入文档提交。
- 敏感信息必须走 `.env`，不得提交仓库。

## 服务器和数据库信息
- 部署服务器：JSCN-233
- SSH 用户：`yvesyuan`
- MySQL 端口：`6603`
- MySQL 用户：`anbo`
- 当前历史数据库名：`anbo_wx`
- 生产平台数据库名：`anbo_wx`；智能装维必须复用该库现有用户、组织、权限和 OSS 绑定字段，不得另起身份库。
- 服务器 Host/IP 未写入仓库；如需连接服务器，每次对话前向用户询问服务器密码和必要连接信息。
- 数据库密码、服务器密码、JWT 密钥、OSS 加密密钥、外部系统 token 一律不得写入仓库。

## 文档规则
- `统一工单池_含OSS融合_技术与实施总规划.md` 是最高优先级主纲。
- `PROJECT_STATUS.md` 是项目当前状态唯一总览。
- `TASK_LOG.md` 是开发流水日志。
- `CHANGELOG.md` 是面向协作者的变更摘要。
- 与主纲冲突的旧文档不再作为依据，只能作为归档参考。

## Git 规则
- 文档任务只提交文档和必要的敏感信息清理，不夹带业务代码。
- 若工作区已有未提交业务改动，必须只暂存本次任务相关文件。
- `git push` 失败时，不要反复乱试；记录失败原因到 `TASK_LOG.md` 并反馈用户。
