# PROJECT_PLAN

## 一、主纲
本项目以 `统一工单池_含OSS融合_技术与实施总规划.md` 为唯一主纲。现有小程序和 Flask 后端作为阶段性基础继续演进，后续目标是统一用户体系、统一工单池、OSS 工单融合、运维工具聚合入口和 Web 管理端。

2026-08-12 补充决策：项目按全新数据库建设，不导入旧库业务数据。旧项目只用于功能和接口分析。数据库 V2、用户外部身份、智能装维和 Web 管理由当前新平台模型重新实现。

## 二、两周内优先顺序
1. 完成文档与项目规范统一，消除旧文档冲突。
2. 优先建立 uni-app 小程序新前端工程，完成登录、菜单、我的和基础管理页骨架。
3. 修正用户体系、权限体系和菜单体系的现有缺陷，并接入 uni-app 前端。
4. 建立统一工单池基础模型和状态动作接口。
5. 分析 OSS 旧逻辑，将 OSS 查询与同步逐步接入统一工单池。
6. 设计服务器设备信息管理模块，作为运维资产台账和快速查询入口，后续与统一工单关联。

## 三、阶段规划

### 1. 文档与项目规范初始化
- 建立根目录统一文档。
- 归档旧文档。
- 明确 Codex 执行纪律、敏感信息规则和 Git 规则。
- 建立 `PROJECT_STATUS.md` 作为项目状态总览。

### 2. 用户登录与权限体系
- 梳理现有 `users`、角色字段和登录流程。
- 明确后续 `roles`、`permissions`、`user_roles`、`role_permissions` 演进路径。
- 修正当前用户管理接口边界问题。
- 固化登录、改密、OSS 绑定、权限校验测试。
- 使用 `username + RBAC + user_org_memberships` 作为新主线；单一角色和单一组织字段仅作兼容。
- OSS 账号从用户主表拆分为外部账号、外部身份和确认绑定关系。

### 3. 菜单体系与小程序基础页面
- 新建 uni-app 小程序工程作为前端主线。
- 现有 `miniprogram/` 原生小程序只作为功能参考，不继续作为长期主线扩展。
- 在 uni-app 中配置原生 tabBar，优先保留“菜单/我的”两个入口。
- 从手写入口逐步收敛为统一菜单入口。
- 菜单从后端动态加载，并按角色权限过滤。
- 移除当前不涉及的注册、忘记密码等入口。

### 4. 小程序用户管理
- 在 uni-app 新工程中重建用户管理页面，避免继续修补旧原生页面造成更大技术债。
- 旧原生用户管理页作为接口和字段参考。
- 补齐新增、编辑、启停、重置密码、筛选、分页。
- 区分 `super_admin` 与 `org_admin` 可见字段和可执行动作。

### 5. 统一工单池基础模型
- 建立 `work_orders` 主表。
- 支持内部工单、OSS 工单和外部系统工单统一入池。
- 将来源差异放入 `source_payload_json`，高频筛选字段结构化。

### 6. 工单筛选、列表、详情、状态流转
- 建立工单列表、详情和筛选接口。
- 工单状态变更必须通过动作接口。
- 写入 `work_order_logs`，禁止前端直接修改 `status`。

### 7. 现场施工记录与评价防伪
- 建立现场记录、照片、定位、备注、评价模型。
- 支持施工记录与工单生命周期关联。
- 评价防伪先预留验证码/短信服务适配器。
- 智能装维按施工实例、施工轮次、五类标准照片、AI运行、综合评分、签字和人工复核分别建模。
- 重新拍摄不删除旧轮次和旧照片，保证审计与历史查询完整。

### 8. 外部系统接入与回调
- 建立外部系统推单入池接口。
- 建立状态更新接口和回调日志。
- 支持 token/HMAC 鉴权模式。

### 9. OSS 旧逻辑分析
- 分析 `docs/oss-reference/` 中已调通的 OSS 登录、查询、详情、状态操作逻辑。
- 输出旧 OSS 字段和统一工单字段映射。
- 不照搬旧结构，只复用知识、请求格式和适配器逻辑。

### 10. OSS 账号绑定与查询
- 完善 OSS 账号绑定、加密存储、校验和解绑。
- 保留 OSS 原始查询能力。
- 查询结果区分“原始 OSS 工单”和“已同步统一工单”。

### 11. OSS 工单同步进统一工单池
- 建立 `source_system=OSS` 的统一入池流程。
- 使用 `external_order_id` 去重。
- 保存 `external_status` 和 `source_payload_json`。
- 支持刷新 OSS 状态和后续状态回推选项。

### 12. 现有网管功能迁移
- 盘点现有水印相机、IP 计算器和后续网管查询工具。
- 将工具迁移到 uni-app 菜单入口聚合，不干扰统一工单主线。
- 需要接入工单的工具必须通过统一工单池关联。

### 13. 服务器设备信息管理
- 建立服务器资产主档、访问账号、加密凭据、端口服务、应用服务、数据库实例、防火墙规则、资产授权和审计日志设计。
- 支持从现有 Word/Excel/CSV 资料生成导入预览，再人工确认入库。
- 支持按名称、IP、端口、账号、应用、数据库、位置、标签快速查询。
- 支持一键生成 SSH、SCP、MySQL、Redis、串口等连接命令；密码查看或复制必须单独授权并写审计。
- 服务器密码、数据库密码、私钥、token 不得进入仓库，不得进入普通备注或日志。
- 后续服务器巡检、故障、变更、账号授权等流程通过统一工单池关联服务器资产。

### 14. Web 管理端基础规划与后续实现
- Web 管理端纳入本次智能装维范围。
- 增加五智能体定义、版本测试和发布管理。
- 增加全量工单、照片、AI结果、人工复核、OSS回单和批量导出管理。
- 使用 `export_jobs / export_job_items` 承担异步批量导出。
