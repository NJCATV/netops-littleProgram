# 项目当前状态

## 一、项目定位
本项目目标是将智维助手升级为统一工单与用户体系平台：统一工单池 + OSS 融合 + 小程序 + 运维工具聚合入口 + 后续 Web 管理端。

OSS 工单必须纳入统一工单池。OSS 查询可以保留原始能力，但最终必须映射到统一工单数据结构。

## 二、当前代码状态

### 当前前端技术栈
- 当前实际工程是微信小程序原生工程，目录为 `miniprogram/`。
- 使用 WXML、WXSS、JavaScript、JSON。
- 已引入 TDesign Miniprogram 组件。
- 新 uni-app 小程序主线工程已创建，目录为 `miniapp/`。
- `miniapp/` 使用 Vue 3 + uni-app，已包含 `src/pages.json`、`src/manifest.json`、`src/App.vue`、`src/main.js`、请求封装和页面骨架。
- 最新决策：前端优先更换为 uni-app 实现。后续新小程序端开发应落到 `miniapp/`，现有原生小程序作为旧实现、功能参考和迁移来源。

### 当前后端技术栈
- 后端目录为 `backend/`。
- 使用 Python Flask、SQLAlchemy、Flask-Migrate、JWT、MySQL。
- 已有 Blueprint 路由：认证、用户管理、组织管理、菜单管理、工作台、文件、健康检查。

### 当前数据库使用情况
- 当前历史数据库名记录为 `anbo_wx`。
- 新平台数据库名已确认为 `zhiwei_assistant`，统一工单池后续迁移面向该库执行。
- 已有模型：`users`、`org_units`、`app_menus`、`login_logs`、`operation_logs`，其中 `users` 已包含 `avatar_url`。
- 已有统一工单池基础模型：`work_orders`、`work_order_logs`、`work_order_comments`，已完成 Task 10 数据层和迁移。
- 已有简化版服务器资产台账、共享可见性和加密连接资料表。
- 尚无独立 `roles`、`permissions`、`user_roles`、`role_permissions`。

### 当前已完成页面
- uni-app 网管中心：已完成移动端整体重建；ONU 历史显示日期/时间并可打开重复记录详情，质差页包含趋势、OLT 排名和端口统计，OLT 设备按区域/机房组折叠，性能页默认不选择采集异常。
- Radius 一键诊断：支持按 GDF/GDC 账号或完整终端 MAC 查询认证成功率、流量、会话、问题线索和 ONU 一致性，并可联查 FTTH ONU。
- AIOps 移动看板：支持查看最新和历史 AI 研判报告、分类结论、关键证据、建议动作、数据新鲜度和来源数据量。
- AI 运维助手：支持故障知识与 AIOps 问答、新建/恢复/删除个人历史会话。
- uni-app 工作台：生产旧菜单路径会合并成“网管 / 便捷工具 / 系统管理”，网管按 ONU、CM/CMTS 优先排序，重复系统入口按实际小程序路径去重。
- BOSS 用户页：仅超级管理员可见，进入前需二次验证当前密码；列表脱敏，完整资料逐条查看，页面隐藏或退出即清空敏感授权和数据。
- uni-app 登录页：已重建，移除注册、忘记密码和免登录入口；只记住上次账号，不保存明文密码，已有有效 token 时自动进入系统，退出登录会清理 token。
- uni-app 菜单页：已重建，使用原生 tabBar 的“菜单”入口，从后端动态加载菜单，并展示用户头像。
- uni-app 我的页：已重建，支持用户资料展示、头像更换、改密入口、OSS 账号确认入口和退出登录。
- uni-app 用户管理页：已重建，支持列表、筛选、新增、编辑、启停、重置密码基础流程。
- uni-app OSS 账号确认页：已重建基础流程。
- uni-app 首次修改密码页：已重建基础流程。
- 旧工作台页：半完成，作为迁移参考保留。
- uni-app 组织管理页：已完成基础重建，支持列表、新增下级、编辑、启停。
- uni-app 功能菜单管理页：已完成基础重建，支持列表、新增、编辑、启停。
- 管理页显示优化：用户、组织、功能管理已改为手机端紧凑工具栏；用户筛选可收起；组织列表支持父子紧邻的展开/折叠树；状态、角色、用户类型、OSS 状态均以中文展示。
- 组织删除：已补齐前端删除入口和后端递归删除接口，支持删除组织及所有下级组织。
- 水印相机：可用工具，后续需考虑与工单现场记录联动。
- IP 计算器：可用工具。
- 服务器设备信息管理：已完成模块设计，计划作为运维资产台账和快速查询入口。

### 当前已完成接口
- `GET /api/health`
- `POST /api/files/avatar`
- `GET /api/files/avatars/{filename}`
- `POST /api/auth/login`
- `GET /api/auth/me`
- `POST /api/auth/bind-oss`
- `POST /api/auth/change-password`
- `POST /api/auth/logout`
- `GET /api/workbench/apps`
- `GET/POST/PUT /api/admin/orgs`
- `GET/POST/PUT /api/admin/menus`
- `GET/POST/PUT /api/admin/users`
- `POST /api/admin/users/{id}/enable`
- `POST /api/admin/users/{id}/disable`
- `POST /api/admin/users/{id}/reset-password`

### 当前能运行的功能
- 网管平台移动端：复用已部署 `/api/netops2026`，支持 ONU/CM 查询、实时光功率、Radius 一键诊断、AIOps 研判、AI 运维问答、质差筛选和导出、OLT 性能、采集状态/任务/历史、OLT/CMTS 设备维护、BOSS 用户查询/导入、设备组织与告警规则。
- uni-app 登录认证基础流程：已接入现有后端接口。
- uni-app OSS 账号校验/绑定基础流程：已接入现有后端接口。
- uni-app 用户管理基础页面：已接入现有后端接口。
- 组织管理基础接口：已补齐 uni-app 页面所需查询、新增、编辑、启用、禁用。
- 菜单管理基础接口：已接入 uni-app 页面，支持查询、新增、编辑、启用、禁用。
- uni-app 动态菜单：已接入现有 `GET /api/workbench/apps`。
- 水印相机：可用。
- IP 计算器：可用。
- 安全基线：新密码使用 scrypt 且至少 12 位，登录失败限流，JWT 默认 8 小时，生产密钥强校验，CORS 白名单和 API `no-store` 已写入代码。

### 当前不能运行或半成品功能
- 统一工单池：已有基础数据层和服务，列表、详情、状态动作接口尚未开发。
- 工单列表、详情、状态流转：不可用。
- OSS 工单同步入统一工单池：不可用。
- 外部系统推单、状态更新、回调：不可用。
- 服务器资产登记、查询、命令生成、共享可见性和凭据查看审计：已具备简化版能力。
- Web 管理端：不可用，尚未规划实现。
- 短信评价防伪：不可用。

### 当前是否有权限体系
- 有角色字段和基础角色过滤。
- 尚无完整 RBAC 表结构。
- `super_admin`、`org_admin`、`normal_user` 已用于部分接口控制。
- 权限体系需要升级为角色、权限、菜单的统一模型。

### 当前是否已有 OSS 逻辑
- 已有 OSS 登录/校验服务和参考代码，当前后端 `POST /api/auth/bind-oss` 会按旧项目已调通格式调用 OSS `/login` 做绑定校验。
- 已有 OSS 账号绑定页面，绑定为非强制行为；未绑定、待确认或校验失败时允许进入系统，小程序每次重新打开后提醒一次，可稍后在“我的”页面绑定。绑定成功默认不覆盖小程序密码，用户勾选“使用 OSS 密码作为小程序登录密码”后才覆盖。
- 已有 `docs/oss-reference/` 中的 OSS 接口参考。
- 尚未完成 OSS 工单查询页面。
- 尚未完成 OSS 工单同步入统一工单池。

### 当前是否已有网管功能
- 已有水印相机。
- 已有网络/IP 地址计算器。
- 已完成 ONU、CM、Radius、AIOps、AI 问答、OLT/CMTS 设备、BOSS 用户、质差、性能、采集、设备组织和规则配置的小程序入口与页面。
- 网管后端继续由 `newGoColletor` 的 `/api/netops2026` 提供；小程序端不直接访问采集数据库。

### 当前项目结构是否混乱
- 存在历史小程序文档与当前后端化方向冲突。
- 存在根目录、`docs/`、`miniprogram/`、`backend/` 多处文档重复。
- 本次已将旧文档归档到 `docs/archive/`。
- 前端已开始从原生小程序迁移到 uni-app，新主线目录为 `miniapp/`，旧 `miniprogram/` 作为参考保留。

### 当前是否存在重复代码
- 暂未系统性审查重复代码。
- 前端表单、底部导航、弹层样式存在重复实现，需要后续抽象。

### 当前是否存在废弃逻辑
- 旧登录页注册、忘记密码、免登录工具入口与当前平台定位不一致；uni-app 登录页已移除这些入口。
- 旧页面手写底部导航导致导航体验异常；uni-app 已改为 `pages.json` 原生 tabBar。
- 旧文档中“不接后台、不接数据库”已过时并归档。

## 三、当前已完成能力
- uni-app 工程骨架：已完成。
- uni-app 登录页：已完成基础重建。
- uni-app 菜单页：已完成基础重建。
- uni-app 我的页：已完成基础重建。
- uni-app 用户管理页：已完成基础重建。
- OSS 账号确认：已完成 uni-app 基础重建。
- 首次修改密码：已完成 uni-app 基础重建。
- 用户管理：已完成 uni-app 基础页面，需要继续完善后端边界和接口测试。
- 组织管理：已完成 uni-app 基础页面和接口接入。
- 菜单管理：已完成 uni-app 基础页面和接口接入。
- 工作台：半完成。
- 我的页面：半完成。
- 水印相机：可用，后续需要与工单现场记录融合。
- IP 计算器：可用。
- 统一工单池：已完成基础模型和迁移，接口和页面不可用。
- OSS 工单同步：不可用。
- 服务器设备信息管理：已完成设计，代码不可用。
- Web 管理端：不可用。

## 四、当前主要问题
- 用户体系仍是过渡模型，缺少完整 RBAC。
- 前端页面结构混乱，登录、工作台、我的、管理页需要统一设计。
- 菜单入口与导航体验不统一。
- 工单核心结构已建立，列表、详情和状态动作接口待实现。
- 服务器管理已完成资产台账、加密凭据管理和 SSH/MySQL 命令生成，不包含远程执行或监控采集。
- OSS 尚未融合进统一工单池。
- 旧 OSS 逻辑还未完成字段映射分析。
- 服务器资产信息可通过小程序页面结构化登记；外部 Word 等人工资料尚需后续导入预览和人工确认流程逐步迁移。
- 文档此前重复且冲突，本次已完成初步归档和统一。
- 当前存在未提交业务代码改动，文档提交时需避免混入。

## 五、当前阶段目标（最近 1~2 周）
当前第一优先级：
1. 文档与规范统一。
2. uni-app 小程序新前端工程初始化。
3. 用户体系。
4. 统一工单池。
5. OSS 融合。
6. 网管功能迁移。

## 六、下一阶段计划
- 完善 uni-app 用户、组织、功能管理页面的分页、权限态、删除/禁用边界和接口级测试。
- 迁移水印相机和 IP 计算器到 uni-app。
- 完成用户管理后端功能收口和接口测试，并对接 uni-app 页面。
- 建立统一工单池数据库迁移和基础模型。
- 建立工单列表、详情和状态动作接口。
- 分析 OSS 旧逻辑，产出字段映射和适配器方案。
- 实现服务器设备信息管理的核心模型、权限、查询和命令生成功能。

## 七、风险与注意事项
- 不允许直接复制旧 OSS 结构。
- 不允许绕过统一工单池。
- 不允许把 OSS 做成独立孤岛。
- Web 管理端暂缓，uni-app 小程序优先。
- 当前原生小程序不是长期主线；迁移时不得直接丢失水印相机、IP 计算器等已有可用能力。
- 服务器密码和数据库密码不得写入仓库。

## 八、工作记录

### 2026-05-26 22:05-22:25 CST
- 工作内容：项目文档重整与规范统一。
- Codex 用户：yvesyuan。
- 主纲确认：`统一工单池_含OSS融合_技术与实施总规划.md`。
- 结果：建立根目录统一文档结构，归档旧文档，记录当前真实代码状态。

### 2026-05-26 22:30 CST
- 工作内容：前端实现路线调整记录。
- Codex 用户：yvesyuan。
- 决策：小程序前端优先更换为 uni-app 实现；现有 `miniprogram/` 原生小程序作为旧实现和迁移参考。

### 2026-05-26 22:45-23:00 CST
- 工作内容：创建 uni-app 小程序新工程骨架。
- Codex 用户：yvesyuan。
- 结果：新增 `miniapp/`，完成登录、菜单、我的、用户管理页面基础重建，补齐改密、OSS 绑定和部分旧菜单路径占位页。
- 验证：`npm run build:mp-weixin` 通过。

### 2026-05-26 23:01-23:12 CST
- 工作内容：修复 uni-app 显示问题并补齐组织、功能管理基础能力。
- Codex 用户：yvesyuan。
- 结果：修复按钮文字垂直位置问题，完成组织管理和功能管理可用页面，补齐组织启用接口。
- 验证：`npm run build:mp-weixin` 通过，`python3 -m compileall backend/app` 通过。

### 2026-05-26 23:25-23:38 CST
- 工作内容：按手机侧视觉优化管理页并统一中文提示。
- Codex 用户：yvesyuan。
- 结果：管理页移除大面积顶部卡片，改为紧凑工具栏；用户筛选收起；组织管理改为可折叠树；功能管理压缩卡片信息；前端状态、角色、用户类型、OSS 状态和常见错误提示中文化。
- 验证：`npm run build:mp-weixin` 通过，`python3 -m compileall backend/app` 通过。

### 2026-05-26 23:45 CST
- 工作内容：修复组织树展开层级体验并补齐删除组织能力。
- Codex 用户：yvesyuan。
- 结果：组织管理改为先序树展示，展开后子级紧跟父级；增加层级提示、路径提示和删除入口；后端新增 `DELETE /api/admin/orgs/{id}` 递归删除接口。
- 验证：`npm run build:mp-weixin` 通过，`python3 -m compileall backend/app` 通过。

### 2026-05-27 08:30 CST
- 工作内容：确认 OSS 登录校验状态并调整 OSS 绑定体验。
- Codex 用户：yvesyuan。
- 结果：保留正式 OSS `/login` 校验作为绑定校验；登录后不再强制跳转 OSS 绑定；绑定页增加“暂不绑定，进入系统”；菜单页对未绑定、待确认或校验失败状态按每次小程序启动提醒一次；绑定成功默认不覆盖小程序密码，用户勾选后才使用 OSS 密码覆盖。

### 2026-05-27 08:50 CST
- 工作内容：补齐登录页记住账号与自动登录体验。
- Codex 用户：yvesyuan。
- 结果：登录成功后保存上次登录账号；登录页自动填入该账号；已有有效 token 时自动校验当前用户并进入系统；用户在“我的”页面退出登录后清理 token，保留上次账号便于重新登录或切换账号。
