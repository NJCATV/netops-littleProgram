# QUESTIONS

## 一、未指定信息
- 旧项目实际路径：未指定。
- 服务器 Host/IP：未写入仓库；连接服务器前需用户提供。
- 服务器密码：不得写入仓库；每次需要连接服务器时向用户询问。
- 生产数据库名称：已确认继续使用智维平台现有 `anbo_wx`。
- 智能装维不新建账号、组织、角色或数据库，直接与现有平台融合。
- OSS 原始状态枚举：待通过旧项目和真实接口分析确认。
- OSS 原始字段名：待通过旧项目和真实接口分析确认。
- OSS 原接口或爬取逻辑细节：已有 `docs/oss-reference/` 参考，但仍需完整分析。
- Web 管理端技术栈：已确认接入现有 `netops-portal-web`。
- uni-app 新工程目录名：已采用 `miniapp/`。
- 短信服务商：待确认。
- 是否第一阶段启用 OSS 状态回推：待确认。
- 服务器资产管理是否允许在小程序端查看/复制密码：待确认。
- 服务器资产凭据查看是否需要二次验证登录密码或审批：待确认。
- 服务器资产是否第一版支持私钥上传和保存：待确认。
- 服务器资产导入后是否保存 Word/Excel 原始附件：待确认；若保存，应作为敏感附件限制下载。
- 是否需要生成 Xshell、FinalShell、MobaXterm 等客户端专用连接配置：待确认。

## 二、当前代码待确认事项
- 当前用户、组织、菜单、认证和 `role_code` 是智能装维的唯一权限基础。
- 当前用户管理前端存在显示和交互问题，需单独任务重构。
- 当前前端路线已调整为优先 uni-app，新页面建议在新工程重建，不继续大修旧原生小程序页面。
- 当前尚无统一工单池模型和接口。
- 当前 OSS 仅有账号校验、绑定参考和旧接口参考，尚未完成统一工单池融合。
- 当前水印相机和 IP 计算器属于工具能力，是否与工单现场记录联动待确认。
- 当前服务器信息主要记录在外部 Word 文档中，包含账号、端口、数据库、应用、防火墙/Nginx 等混合信息；后续导入必须先预览和人工确认，敏感凭据不得进入普通备注。

## 三、决策建议
- uni-app 小程序工程已新建在 `miniapp/`，现有微信小程序原生工程作为迁移参考保留。
- 数据库已确认复用生产 `anbo_wx`，上线前后需校验用户与 OSS 绑定不变。
- OSS 状态回推建议第二阶段再启用，第一阶段先完成查询和同步入池。
- 服务器资产管理建议先做资产台账、快速查询、命令生成和权限审计；巡检、故障、变更、账号授权再通过统一工单池关联。
